// Package envfile provides utilities for parsing, writing, and managing
// .env files. The rename submodule allows keys to be renamed in-place
// within an env map or directly within a file on disk.
package envfile
